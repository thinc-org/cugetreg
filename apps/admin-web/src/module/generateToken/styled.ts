import { styled } from '@mui/material'

export const SpinnerContainer = styled('div')({
  display: 'flex',
  height: '100vh',
  flexWrap: 'wrap',
  justifyContent: 'center',
  alignContent: 'center',
})
