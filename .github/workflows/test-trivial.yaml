name: Test trivial

on:
  push:
    branches: [mvp1-dev]

jobs:
  caller:
    uses: ./.github/workflows/trivial-reusable.yaml
    with:
      greeting: hello
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
