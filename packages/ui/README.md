# UI

UI Components for CU Get Reg and others app like Backoffice.

## Adding a components

When adding a new component for shadcn-svelte cmd, the component will appear under src/ui, please move it to the correct place under src/components.

Make sure the input for utils is `@repo/ui/utils` (No `.js`)
