import { styled } from '@material-ui/core'

export const Container = styled('div')({
  // TODO: add responsive size
  width: '100%',
  maxWidth: 1024,
  paddingLeft: 8,
  paddingRight: 8,
  margin: '0 auto',
})
