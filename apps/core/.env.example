# ── Database ──
DB_HOST=db
DB_PORT=5432
DB_USER=USER_NAME
DB_PASS=PASSWORD
DB_NAME=DB_NAME
# DATABASE_URL="postgresql://${DB_USER}:${DB_PASS}@localhost:${DB_PORT}/${DB_NAME}?schema=public"
DATABASE_URL="postgresql://USER_NAME:PASSWORD@localhost:5432/DB_NAME?schema=public"

# ── App config ──
APP_MODE=dev

# ── Auth & secrets ──
BETTER_AUTH_SECRET=ABCdsfsvsdkjvdakj
# Must include /api/v1/auth — better-auth defaults to expecting /api/auth
# otherwise, which doesn't match where index.ts mounts the auth routes.
BETTER_AUTH_URL=http://localhost:3000/api/v1/auth
BETTER_AUTH_TRUSTED_ORIGINS=http://localhost:5173
INTERNAL_API_TOKEN=change-me-to-a-random-32-char-value

# ── Google OAuth ──
GOOGLE_CLIENT_ID=123-XXX.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=BBBBBBB
