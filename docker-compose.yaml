version: '3.9'

services:
  webapp:
    build: ./
    image: cugetreg-frontend:latest
    container_name: cugetreg-frontend
    restart: unless-stopped
    ports:
      - 3000:3000
    networks:
      - cugetreg-network

networks:
  cugetreg-network:
    name: cugetreg-network
